# Dependencies
node_modules/

# Build output
dist/
*.crx
*.pem
*.zip

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local env / secrets
.env
.env.local

# A nested clone of this same repository was committed by mistake as a broken
# submodule gitlink. Keep it untracked.
GITHUB/

# Suggested default BAT workspace folder (Settings -> Workspace folder). Collected
# data belongs here, not in the repo — keep everything except the explainer out of git.
workspace/*
!workspace/README.md
